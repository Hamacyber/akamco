name: Deploy to Azure App Service

on:
  push:
    branches: [main, master, production]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run linting (optional)
        run: npm run lint || true
        continue-on-error: true

      - name: Build Next.js application
        run: npm run build
        env:
          NODE_ENV: production

      - name: Verify build output - Immediately after npm run build
        run: |
          echo "=== STEP 1: Verify .next directory exists after npm run build ==="
          test -f .next/server/font-manifest.json && echo "✓ .next/server/font-manifest.json EXISTS" || echo "✗ .next/server/font-manifest.json MISSING"
          echo ""
          echo "=== .next directory listing ==="
          ls -la .next
          echo ""
          echo "=== .next/server directory listing ==="
          ls -la .next/server

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: .
          include-hidden-files: true
          retention-days: 1

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Verify artifact contents - Immediately after download
        run: |
          echo "=== STEP 2: Verify .next directory exists after artifact download ==="
          test -f .next/server/font-manifest.json && echo "✓ .next/server/font-manifest.json EXISTS in artifact" || (echo "✗ .next/server/font-manifest.json MISSING in artifact" && exit 1)
          echo ""
          echo "=== .next directory listing from artifact ==="
          ls -la .next
          echo ""
          echo "=== .next/server directory listing from artifact ==="
          ls -la .next/server

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_59BA7BADE6614468AD773A142021BEED }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_92889C17779741F9B2F4ACF43D06EFDF }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_596D86DBDCA048599B91E409327AC8B3 }}

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'akamco-webapp2'
          slot-name: 'Production'
          package: .

      - name: Notify deployment success
        if: success()
        run: echo "✅ Successfully deployed to Azure!"

      - name: Notify deployment failure
        if: failure()
        run: echo "❌ Deployment failed. Check logs above."
